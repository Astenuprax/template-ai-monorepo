"""example-mcp-server: governance-audit MCP server (stdio).

Kept import-light so ``python -m example_mcp_server.server`` does not double-import
the module under runpy. Import the server explicitly where needed:
``from example_mcp_server.server import mcp, main``.
"""
